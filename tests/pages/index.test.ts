import React from 'react';
import { render, screen } from '../test-utils';
import HomePage from '../../pages';

describe('HomePage', () => {
  it('renders', () => {
    // @ts-ignore
    render(HomePage);
  });
});
